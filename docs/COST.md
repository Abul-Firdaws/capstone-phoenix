# Infrastructure Cost

The cluster runs on three small cloud instances.

Estimated monthly cost:

$15-$30 depending on provider and instance size.

Cost reduction options:

- Use smaller instances.
- Use spot instances.
- Use cheaper Kubernetes providers.
- Reduce worker count for development environments.

# DO NOT fill in real values here and DO NOT commit real values to git.
# This file only documents which keys the Secret needs.
# Create the REAL Secret directly on the cluster with the kubectl command
# in RUNBOOK.md — never as a committed YAML file.
apiVersion: v1
kind: Secret
metadata:
  name: taskapp-secret
  namespace: taskapp
type: Opaque
stringData:
  POSTGRES_USER: CHANGE_ME
  POSTGRES_PASSWORD: CHANGE_ME
  DB_USER: CHANGE_ME
  DB_PASSWORD: CHANGE_ME
  SECRET_KEY: CHANGE_ME
